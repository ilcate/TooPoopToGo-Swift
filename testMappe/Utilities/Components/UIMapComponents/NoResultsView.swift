
import SwiftUI

struct NoResultsView: View {
    var body: some View {
        Text("No results")
            .normalTextStyle(
                fontName: "Manrope-SemiBold",
                fontSize: 16,
                fontColor: .cLightBrown
            )
    }
}
